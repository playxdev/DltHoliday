# ============================================
# Database Connection
# ============================================
# Go-style DSN (used by worker v1.0.7+)
SQLSERVER_DSN="sqlserver://sa:PlayDevX@@001300@146.190.82.168:1433?database=DLT&encrypt=disable"

# OR individual parameters (DB_SERVER takes priority if set)
DB_SERVER="10.0.0.5"
DB_PORT="1433"
DB_NAME="DLT"
DB_USER="sa"
DB_PASSWORD="PlayDevX@@001300"

# ============================================
# Application
# ============================================
NEXT_PUBLIC_APP_NAME="DLT Holiday Admin"
NEXT_PUBLIC_APP_URL="http://localhost:3000"
# Set to your API server URL when deploying frontend separately (Cloudflare Pages)
# Leave empty for same-origin deployment
NEXT_PUBLIC_API_URL=""

# ============================================
# Authentication
# ============================================
AUTH_USERNAME="admin"
AUTH_PASSWORD="your_admin_password"
AUTH_SECRET_TOKEN="your_security_token"
JWT_SIGNING_KEY="your_jwt_signing_key"

# ============================================
# Cloudflare Pages (frontend-only deploy)
# ============================================
# Set these ONLY on Cloudflare Pages (NOT on Vercel):
# CLOUDFLARE_BUILD="true"
# NEXT_PUBLIC_API_URL="https://dlt-holiday-admin.vercel.app"

# ============================================
# Vercel (API server) — set ONLY on Vercel:
# ============================================
# CORS_ORIGIN="https://your-app.pages.dev"
# (Required when frontend is on Cloudflare Pages)
